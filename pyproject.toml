[project]
name = "thesis-counter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.10.0",
    "typer[all]>=0.9.0",
]

[project.optional-dependencies]
gui = ["tkinterdnd2>=0.3.0"]
dev = ["pytest>=7.0", "pytest-cov"]
